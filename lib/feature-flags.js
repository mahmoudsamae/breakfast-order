/**
 * @deprecated Use getRegistrationEnabled() from lib/platform-settings.js (Root toggle).
 * Kept only as compile-time fallback when platform_settings is unavailable.
 */
export const SHOW_REGISTRATION_UI = false;